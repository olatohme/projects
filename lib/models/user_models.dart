class user{
  String ? first_name;
  String ? last_name;
  String ? email;
  String ? password;
  String ? confirmpassword;
  String  ? phone;
  String ? image;
  user({
    this.image,
    this.first_name,
    this.last_name,
    this.email,
    this.password,
    this.confirmpassword,
    this.phone,
  });

}