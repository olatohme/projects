class provider{
  String ? name;

  String ? email;
  String ? password;
  String ? confirmpassword;
  String  ? phone;
  provider({
    this.name,

    this.email,
    this.password,
    this.confirmpassword,
    this.phone,
  });

}