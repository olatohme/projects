class create_order{
  String ? notes;
  String ? payment_methode;
  String ? shipping_method;
  String ? delivery_to;

  create_order({
    this.notes,
    this.payment_methode,
    this.shipping_method,
    this.delivery_to,

  });

}